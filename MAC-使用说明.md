# qianyuniao-Canvas · macOS 一键启动说明

## 快速开始

### 方法一：双击运行（推荐）

1. **首次使用**：右键点击 `mac-启动服务.command` → 选择 "打开" → 点击 "打开"（绕过安全警告）
2. **之后使用**：直接双击 `mac-启动服务.command` 即可

### 方法二：终端运行

```bash
cd 项目文件夹
python3 main.py
```

## 功能特点

- **自动检测依赖**：首次运行会自动检查并安装所需依赖
- **自动打开浏览器**：服务启动后 3 秒自动打开网页
- **显示访问地址**：同时显示本地地址和局域网地址

## 系统要求

- macOS 10.14 或更高版本
- Python 3.10+（如果没有安装，程序会提示下载链接）

## 安装 Python

如果系统没有 Python，请访问 https://www.python.org/downloads/ 下载安装。

## 文件说明

| 文件 | 说明 |
|------|------|
| `mac-启动服务.command` | 双击运行的启动器（自动修复权限、清理旧进程、启动服务） |
| `mac-修复权限.command` | 单独修复 macOS 安全限制 |
| `main.py` | 服务主程序（直接 `python3 main.py` 即可启动） |
| `安装即梦CLI.command` | 安装/更新 dreamina CLI |
| `登录即梦CLI.command` | 登录 dreamina CLI |

## 界面与功能

启动后左侧侧栏从上到下：

| 入口 | 说明 |
|------|------|
| **本地功能**（可折叠） | 文生图 / 细节增强 / 图片编辑 / 角度控制 —— 四个本地页面 |
| **在线生图** | 走在线 API 的图像生成 |
| **GPT 对话** | LLM 对话页 |
| **无限画布 / 智能画布** | 节点式画布，工作流编辑与批量生成 |
| **素材库** | 统一管理生成结果，支持批量采集 |

底部设置面板（紫色区域）：

| 入口 | 说明 |
|------|------|
| **API 设置** | 配置请求地址 / 协议 / Key |
| **更多设置** | 白天·黑夜模式切换主题 · 中文·English 切换语言 · 工作流设置（本地 ComfyUI） |
| **项目主页** | 打开项目仓库 |

> 侧栏一级项（如「本地功能」「在线生图」）与二级项（如「文生图」「细节增强」）做了明确的层级区分：一级是较大的玻璃卡片，二级是内嵌的扁平小卡，展开后会看到一条竖向引导线串起各子项。

## 配置生成渠道

三种模式任选一种即可使用，也可以同时配置：

| 模式 | 配置位置 | 说明 |
|------|----------|------|
| **在线 API** | API 设置 | 填请求地址 + 协议 + Key，保存后可拉取模型列表 |
| **ModelScope** | API 设置 | 填 ModelScope 的 Key，可免费调用图像 / LLM 模型，也支持 LoRA |
| **本地 ComfyUI** | 更多设置 → 工作流设置 | 支持导入自定义工作流 JSON |

配置完成后回到对应页面点「生成」即可。更详细的分步教程见 `新手运行与使用教程.md`。

## 常见问题

### 1. 提示 "无法打开，因为无法验证开发者"

**解决方法**：
- 右键点击 `mac-启动服务.command`
- 选择 "打开"
- 在弹出的对话框中点击 "打开"

或者到 系统设置 → 隐私与安全性 → 安全性 → 点击 "仍要打开"

### 2. 提示 "Python 3 not found"

请安装 Python 3.10 或更高版本：
https://www.python.org/downloads/

### 3. 依赖安装失败

尝试手动安装依赖：
```bash
pip3 install -r requirements.txt
```

## 手动控制

### 安装依赖
```bash
pip3 install -r requirements.txt
```

### 启动服务
```bash
python3 main.py
```

## 访问服务

- 本机访问：http://127.0.0.1:5050/
- 局域网访问：http://你的IP:5050/（启动时会显示）

## 停止服务

在终端窗口中按 `Ctrl+C` 即可停止服务。
